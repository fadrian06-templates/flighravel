{
  "require": {
    "php": "^8.2",
    "flightphp/core": "^3.14"
  },
  "scripts": {
    "pre-install-cmd": [
      "composer check-platform-reqs"
    ],
    "pre-update-cmd": [
      "composer check-platform-reqs"
    ],
    "serve": "php -S 0.0.0.0:61001"
  },
  "config": {
    "sort-packages": true
  }
}
